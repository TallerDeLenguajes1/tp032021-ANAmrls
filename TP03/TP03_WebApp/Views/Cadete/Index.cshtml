@{
    ViewData["Title"] = "Home Page";
}
@model List<TP03_WebApp.Entidades.Cadete>

<div class="text-center">
    <h1 class="display-4">Cadetes</h1>
    @foreach (var item in Model)
    {
        @item.Nombre;
        @item.Id;
    }
</div>
